# Claude Code local/machine-specific overrides (never commit)
.claude/settings.local.json

# Raw geospatial source data — process locally, commit only the baked output
data/raw/
*.tif
*.tiff
*.shp
*.dbf
*.shx
*.prj

# Species-passport seed data — project knowledge only, never the repo (v16.59 item 13)
guya_species_qld_v3.md

# Contaminated pre-drop-mask CSVs — superseded by v2, must not re-enter the repo (v16.59)
*_intertidal_ground_v1.csv
scratchpad/
